---
title: "U"
author: "Florian Hartig"
date: "14 Jan 2016"
output: html_document
---

```{r}


library(PhyloSim)

size = 50

par <- createCompletePar(x = size, y = size, dispersal = 1, runs = 5000, density = 0, environment = 0.5, specRate = 1)

system.time(simu <- runSimulation(par))

plotSpatialPhylo(simu, plot = "both")

SAC1 <- rep(NA, 1:size)
SAC2 <- rep(NA, 1:size)

for (i in 1:size)



library(fields)
image.plot(simu$Output[[1]]$envMat)
simu$Output[[1]]$specMat



```

